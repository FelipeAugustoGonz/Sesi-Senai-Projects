# EcoFlutuador POC - Python Dependencies
# No Raspberry Pi, prefira instalar via apt:
# sudo apt update && sudo apt install -y python3-opencv python3-numpy python3-serial python3-yaml python3-flask

# Para instalar via pip (se necessário):
# pip install -r requirements.txt

pyserial>=3.5
pyyaml>=6.0
flask>=2.0  # apenas para API /status (opcional)

# Nota: opencv-python e numpy devem vir do sistema no RPi (python3-opencv)
# Em desenvolvimento no Windows/macOS, pode instalar via pip:
# opencv-python>=4.8
# numpy>=1.24